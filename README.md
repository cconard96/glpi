# Headless GLPI

It is GLPI... but headless. The frontend functionality is removed as much as possible and the API and Command Line is the only intended way to interact with it.
It is designed to be used with the GLPI Vue frontend application.

This is done to improve maintainability, allow these two distinct applications to evolve independently, and force a separation that allows the easier optimization of each side.
